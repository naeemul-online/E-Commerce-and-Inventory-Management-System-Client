import AdminFeaturePlaceholder from "@/app/(protected)/admin/dashboard/_components/admin-feature-placeholder"

const InventoryPage = () => {
  return (
    <AdminFeaturePlaceholder
      title="Inventory"
      description="Monitor stock levels, adjust quantities, and keep inventory synced with product availability."
    />
  )
}

export default InventoryPage
