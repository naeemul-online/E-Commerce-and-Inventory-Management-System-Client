import AdminFeaturePlaceholder from "@/app/(protected)/admin/dashboard/_components/admin-feature-placeholder"

const OrdersPage = () => {
  return (
    <AdminFeaturePlaceholder
      title="Orders"
      description="Review order lifecycle, update statuses, and manage fulfillment issues from a central queue."
    />
  )
}

export default OrdersPage
