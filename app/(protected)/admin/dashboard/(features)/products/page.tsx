import AdminFeaturePlaceholder from "@/app/(protected)/admin/dashboard/_components/admin-feature-placeholder"

const ProductsPage = () => {
  return (
    <AdminFeaturePlaceholder
      title="Products"
      description="Create, edit, and organize catalog products with structured attributes, pricing, and publish controls."
    />
  )
}

export default ProductsPage
